import 'package:flutter/material.dart';

class Tab6 extends StatelessWidget {
  const Tab6({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Lets Restart"),
    );
  }
}
